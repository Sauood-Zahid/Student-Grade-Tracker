projection;
strict ( 2 );
use draft; // 1. Draft use karne ki command

define behavior for ZC_STUDENT_GRADES alias StudentGrades
{
  use create;
  use update;
  use delete;

  // 2. Expose standard draft actions for UI
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
  use action Resume;
}